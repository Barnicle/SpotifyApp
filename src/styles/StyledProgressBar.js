import styled from 'styled-components';
const StyledProgressBar = styled.input`
  position: absolute;
  width: 400px;
  height: 4px;
  border-radius: 5px;
  background: linear-gradient(to right, red 0 20%, black 20% 100%);
`;
